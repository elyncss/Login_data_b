import "./header.scss";

const Header = () => {
    return (
        <div className="header">
            <h1>Logo</h1>
            <button className="btn">logout</button>
        </div>
    );
};

export default Header;